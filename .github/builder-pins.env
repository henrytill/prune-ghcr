# The buildx and buildkit versions every build of the action's image uses.
#
# Both decide the bytes a build produces, so publishing and every rebuild that
# checks a published digest have to use the same ones. They live in a file
# rather than on the setup-buildx-action step that consumes them because three
# workflows consume them, and the two that did not declare them used to recover
# them by parsing the one that did.
#
# Read with script/builder-pins, which strips these comments. Neither is managed
# by Dependabot: the github-actions ecosystem bumps `uses:` SHAs, and the docker
# ecosystem reads the Dockerfile, so both of these are bumped by hand -- here,
# and nowhere else.
#
# A change here changes the image, so this file is on the build-input list in
# script/verify-digest.

BUILDKIT_IMAGE=moby/buildkit:v0.32.2@sha256:28a898719c18a33f4e8000685287fa36fd0dd9560c6440227d3a732d79bb41d8
BUILDX_VERSION=v0.36.1
